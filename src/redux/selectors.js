export const getContacts = state => state.contacts;

export const getFilter = state => state.filter;

export const gettIsLoading = state => state.isLoading;

export const getError = state => state.error;